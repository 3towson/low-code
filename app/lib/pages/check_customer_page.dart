import 'package:flutter/material.dart';

/// ยังว่าง จะทำในส่วนถัดไป
class CheckCustomerPage extends StatelessWidget {
  const CheckCustomerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('ตรวจสอบลูกค้า')));
  }
}
